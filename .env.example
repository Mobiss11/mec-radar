# Database
POSTGRES_PASSWORD=changeme
DATABASE_URL=postgresql+asyncpg://trader:changeme@localhost:5432/memcoin_trader
REDIS_URL=redis://localhost:6380/0

# Helius (Solana RPC + WebSocket for Meteora DBC)
HELIUS_API_KEY=
HELIUS_RPC_URL=
HELIUS_WS_URL=

# Solana RPC fallback (if helius_rpc_url empty)
SOLANA_RPC_URL=

# Telegram Bot (alerts)
TELEGRAM_BOT_TOKEN=
TELEGRAM_ADMIN_ID=0

# Trading
TRADING_ENABLED=false
PAPER_TRADING_ENABLED=true

# Birdeye Data Services ($39/mo — PRIMARY data source)
BIRDEYE_API_KEY=
BIRDEYE_MAX_RPS=10.0
ENABLE_BIRDEYE=true

# Chainstack gRPC ($49/mo — sub-second token discovery)
CHAINSTACK_GRPC_ENDPOINT=
CHAINSTACK_GRPC_TOKEN=
ENABLE_GRPC_STREAMING=true

# Vybe Network (Developer plan — 500K credits/mo)
VYBE_API_KEY=
ENABLE_VYBE=true

# TwitterAPI.io ($10/1M credits — KOL detection, viral tweets)
TWITTER_API_KEY=
ENABLE_TWITTER=true

# RapidAPI — Telegram checker ($15/mo)
RAPIDAPI_KEY=
ENABLE_TELEGRAM_CHECKER=true

# OpenRouter LLM (~$0.03/1M input — token risk analysis)
OPENROUTER_API_KEY=
ENABLE_LLM_ANALYSIS=true

# SolSniffer (paid plan — 5000 calls/mo)
SOLSNIFFER_API_KEY=
ENABLE_SOLSNIFFER=true

# Free services (feature flags)
ENABLE_GMGN=true
ENABLE_PUMPPORTAL=true
ENABLE_DEXSCREENER=true
ENABLE_METEORA_DBC=true
ENABLE_WEBSITE_CHECKER=true

# GMGN config
GMGN_PARSE_INTERVAL_SEC=60
GMGN_MAX_RPS=1.5

# DexScreener config
DEXSCREENER_MAX_RPS=4.0

# GMGN proxy (rotating residential)
GMGN_PROXY_URL=

# Enrichment workers (parallel consumers, default 3)
ENRICHMENT_WORKERS=3
